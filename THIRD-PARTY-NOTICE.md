# 원본 및 제3자 파일

사용자가 제공한 구매 패키지: `Floka_FullPackage.zip`.

기준 데모: https://floka.casethemes.net/

출력물에 포함된 Floka, Case Addons, Elementor, GSAP, Swiper, jQuery, 폰트, 이미지, 영상 및 기타 제3자 자산의 권리는 해당 권리자에게 있습니다. 이 프로젝트는 포함 파일을 새 라이선스로 재배포하거나 라이선스 권한을 추가하지 않습니다. 원본 코드의 라이선스 주석을 보존했습니다.

`capture-manifest.json`의 `source` 필드는 구매 패키지에서 가져온 파일(`purchased-package`)과 공개 데모에서 확보한 파일(`live-demo`)을 구분합니다. 각 파일의 URL, 경로, 크기, SHA-256을 함께 기록했습니다.

구매 패키지에 있던 PHP/관리자 서버를 실행하는 프로젝트는 아닙니다. 화면에 필요한 정적 프런트엔드 파일을 사용하는 구조입니다.
