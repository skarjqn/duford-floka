{
  "name": "floka-frontend-export",
  "version": "1.0.0",
  "private": true,
  "description": "Original Floka frontend snapshot; no redesign or WordPress server required.",
  "scripts": {
    "build": "python3 scripts/build.py",
    "start": "python3 scripts/build.py && python3 -m http.server 8080 --directory site"
  }
}
